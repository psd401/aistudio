{
  "name": "psd-workflows",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw skill for dynamically discovered PSD Agent Gateway workflows through the owner-bound AI Studio broker.",
  "main": "run.js",
  "scripts": {
    "test": "node --test"
  }
}
