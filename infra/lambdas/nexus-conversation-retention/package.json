{
  "name": "nexus-conversation-retention",
  "version": "1.0.0",
  "description": "Nightly retention sweep — hard-deletes Nexus conversations whose last message is older than NEXUS_CONVERSATION_RETENTION_DAYS, unless flagged Keep or pinned. No-ops when the setting is empty/0 (Issue #1330).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prebuild": "rm -rf dist",
    "test": "bun test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
