{
  "name": "agent-scheduler-sync",
  "version": "1.0.0",
  "description": "Reconciles EventBridge Scheduler schedules against the psd-agent-schedules DynamoDB table via DDB Streams. One row = one schedule.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prebuild": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-scheduler": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
