{
  "name": "agent-chat-bridge",
  "version": "1.0.0",
  "description": "Forwards Google Chat Pub/Sub push deliveries to the agent-router SQS queue",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prebuild": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
